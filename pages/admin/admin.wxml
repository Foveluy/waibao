<!--pages/admin/admin.wxml-->
<web-view src="https://zh.9uhxir.top/uploads/admin/search.html"></web-view>
<!-- <view class="wrap">
  <view class="gpa" wx:for="{{trainer}}" wx:key="index">
    <view class="name">{{item.trainer}}</view>
    <view class="performance">约课业绩：{{item.gpa}}次</view>
  </view>
</view>



<view class="vip" wx:for="{{user}}" wx:key="index">
  <view class="icon">
    <image class="user-avatar" mode="widthFix" src="{{item.avatarUrl}}"></image>
  </view>
  <view>
    <view class="user"> {{item.name}}</view>
    <view class="credit"> {{item.credit}}</view>
  </view>

  <input confirm-type="充值" class='input' id='{{item.openid}}' bindconfirm='topup' placeholder="充值" value="{{value}}" />

  <input confirm-type="充值" class='input' id='{{item.openid}}' bindconfirm='unitPrice' placeholder="单:{{item.unitPrice}}" value="{{unitPrice}}" />
</view> -->